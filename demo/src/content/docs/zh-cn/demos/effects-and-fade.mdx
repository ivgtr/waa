---
title: 效果与淡化
description: 增益、滤波器和淡入/淡出的交互式演示
---

import EffectsDemo from '../../../../components/demo/EffectsDemo.astro';

音频效果链和淡化功能的演示。

- **增益**: 实时调整音量
- **滤波器**: 在低通/高通/带通之间切换，调整频率和 Q 值
- **淡化**: 执行淡入/淡出

<EffectsDemo locale="zh-cn" />

## 代码示例

### 效果链

```ts
import { WaaPlayer } from "waa-play";

const player = new WaaPlayer();
const buffer = player.createSineBuffer(440, 3);

const gain = player.createGain(0.8);

const filter = player.ctx.createBiquadFilter();
filter.type = "lowpass";
filter.frequency.value = 1000;
filter.Q.value = 5;

player.chain(gain, filter, player.ctx.destination);

const playback = player.play(buffer, {
  through: [gain],
  loop: true,
});

// 实时修改参数
filter.frequency.value = 500;
gain.gain.value = 0.5;
```

### 淡化

```ts
// 淡入（0 → 当前增益值）
player.fadeIn(gain, 0.5); // 0.5 秒

// 淡出（当前增益值 → 0）
player.fadeOut(gain, 1.0); // 1.0 秒
```
